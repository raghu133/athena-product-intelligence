.env
__pycache__/
*.pyc
.pytest_cache/
# NOTE: athena/data/store/ is intentionally committed with a prebuilt index so
# the hosted demo loads instantly without re-embedding (Gemini free-tier has a
# 100 req/min embedding cap). Delete it and rebuild locally to regenerate.
.chroma/
*.log
.venv/
venv/
.DS_Store
.streamlit/secrets.toml
