# Streamlit Cloud secrets template.
# On Streamlit Community Cloud, paste this (with your real key) into:
#   App → Settings → Secrets
# Locally, copy to .streamlit/secrets.toml (gitignored) if you prefer secrets over .env.

GEMINI_API_KEY = "your-key-here"
